telegram:
  bot_token: "token"
  admin_ids: [1,2,3,4]
  bot_username: "your_bot_username"

postgres:
  host: "localhost"
  port: 5432
  user: "tma_user"
  password: "tma_password"
  db: "tma_db"

auth:
  secret_key: "secret"
  algorithm: "HS256"
  access_token_expire_minutes: 30

log:
  level: "INFO"
